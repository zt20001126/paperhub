# 我要应助功能需求文档

## 核心需求

- 用户在文献详情页点击“我要应助”后，只能选择并提交 `.pdf` 文件。
- 提交时必须登录，后端不信任前端传入的 `userId`。
- PDF 文件保存到 MinIO，数据库记录应助信息，供后续审核、下载、采纳使用。

## 范围

- 本次实现应助 PDF 上传和记录保存。
- 本次不实现发布者下载、审核、采纳和积分结算。

## 验收标准

- 非 PDF 文件在前端和后端都会被拒绝。
- 有效 PDF 提交后，MinIO 出现对象，`literature_assist` 表出现记录。
- 文献求助从待处理状态更新为处理中。
